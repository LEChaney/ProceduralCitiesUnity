using UnityEngine;
using System.Collections;
using System;
using System.Runtime.Serialization.Formatters.Binary;
using System.IO;
using System.Collections.Generic;


    public class convertJson : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

           // Segmentj road = LoadJson.LoadJsonFromFile();
            //Debug.Log(road);
            //Debug.Log(road.getend().x);
            //Debug.Log(road.getend().y);
            //Debug.Log(road.getstart().x);
            //Debug.Log(road.getstart().y);
            //Debug.Log(road.getList());


        }

        // Update is called once per frame
        void Update()
        {

        }
    }

